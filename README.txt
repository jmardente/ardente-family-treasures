PIXEL-MATCH VERSION

The homepage uses the supplied reference image as the exact visual layout, with invisible clickable links placed over all navigation and buttons. This is the closest possible match to the supplied image.

Upload:
- all HTML files
- styles.css
- the complete assets folder

Keep everything in the root of the GitHub repository.
